export default class Plop{
    constructor(name, buttonImage, texture, ID){
        this.name = name;
        this.buttonImage = buttonImage;
        this.texture = texture;
        this.ID = ID;
    }
}